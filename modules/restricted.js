const fs = require('fs-extra');

module.exports = {
  // eval: (client, msg, prefix, args) => {
  // if (msg.author.id === '399596706402009100') {
  //   eval(msg.content.slice(prefix.length + 5))
  // } else {
  //   msg.channel.send('Nice try.');
  // }
  // }
};